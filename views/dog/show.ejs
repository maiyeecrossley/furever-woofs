<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

    <h1 class="show-name"><%= dog.name %></h1>

    <main>
      <img class="show-image" src="<%= dog.image %>" width="300px" alt="<%= dog.name %> the <%= dog.breed.join(", ") %>">
      <p>Breed: <%= dog.breed.join(", ") %></p>
      <p>Age: <%= relativeAge %></p>
      <p>Gender: <%= dog.gender %></p>
      <p>Location: <%= dog.location %></p>
      <p>Description: <%= dog.description %></p>
      <p>Can live with cats? <%= dog.live_with_cats ? "Yes ✅" : "No ❌" %></p>
      <p>Can live with dogs? <%= dog.live_with_dogs ? "Yes ✅" : "No ❌" %></p>
      <p>Can live with children? <%= dog.live_with_children ? "Yes ✅" : "No ❌" %></p>
      <p>Charity: <%= dog.charity_name %></p>
      <p>Reference: <%= dog.reference %></p>
    </main>

    <% if (user && user.user_type === "charity") { %>
      <p><a href="/dogs/update/<%= dog._id %>">Update <%= dog.name %>'s Details</a></p>

      <form action="/dogs/<%= dog._id %>?_method=DELETE" method="POST">
        <button type="submit"><%= dog.name %> has found their furever sofa 🛋️ ❤️</button>
      </form>

    <% } %>

</body>
</html>